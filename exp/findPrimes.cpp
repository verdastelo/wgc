#include <iostream>
#include <vector>

using namespace std;

int main() {
  int initial = 2;
  int final = 1000;

  vector primes = prime[];

  while (initial < final) {
    for (int i = 2; i <= initial/2; i++) {
      if (initial%i != 0) 
        prime.push_back[initial];
    initial++
    }
  }
  return 0;
}
 
