/*
  Write a program to remove trailing blanks and tabs from each
  line of input, and to delete entirely blank lines.
*/
#include <stdio.h>

int getline(char line[], int maxline);

int main (void) {
  return 0;
}

void 
// Any trailing blank or tab will be bounded by a new line.
// A blank line remover will essentially look for single newline chars.
