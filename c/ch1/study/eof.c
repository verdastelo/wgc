#include <stdio.h>

/* Verify that the expression getchar() != EOF is 0 or 1. */

main() {
  int c;
  c = (getchar() != EOF);
  printf ("%d\n", c);
  putchar (c);
}

main() {
  int c;
  while /*


main () {
// An integer c is declared.
  int c;
// c either has the value 1 or 0. 
  while ((c = getchar()) != EOF) {
// c's value is printed.
    printf ("%d ", c != EOF);
// The input char is printed.
    putchar (c);
    printf ("\n");
  }
// c's value is printed.
  printf ("\n%d\n", c != EOF);
}
*/ 
